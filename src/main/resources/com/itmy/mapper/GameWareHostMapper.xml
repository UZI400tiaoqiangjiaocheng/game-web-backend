<!--定义Mapper映射文件的约束和基本结构-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itmy.mapper.GameWareHostMapper">
    <!--根据游戏名称查询游戏-->
    <select id="queryGame" resultType="com.itmy.pojo.vo.GameWareHostVo">
        select gh.id, gh.game_id,gh.user_id,g.game_name,g.game_size,g.game_download_link,g.game_extract_code,g.game_cover from gamewarehouse gh join game g on gh.game_id = g.id
        <where>
            <if test="gameName != null and gameName != ''">
                and g.game_name like concat('%',#{gameName},'%')
            </if>
        </where>
    </select>
</mapper>