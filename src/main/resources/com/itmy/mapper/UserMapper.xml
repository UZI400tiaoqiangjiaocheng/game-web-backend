<!--定义Mapper映射文件的约束和基本结构-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itmy.mapper.UserMapper">

    <insert id="insert">
        insert into user (username, password, account, phone, company, user_type) values
        (#{username}, #{password}, #{account}, #{phone}, #{company}, #{userType})
    </insert>
</mapper>